Package imports:

- numpy as `np`
- scipy as `scipy`
- pandas as `pd`
- matplotlib.pyplot as `plt`
- seaborn as `sns`

and creates `autorequirements.txt` file with versions of installed package versions.